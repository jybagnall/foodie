# 의존성 폴더 (로컬에서만 필요, 컨테이너 안에서는 npm ci로 설치됨)
node_modules

# Dockerfile 자체는 보통 복사하지 않아도 되므로 제외
Dockerfile
.dockerignore

# Git 관련
.git
.gitignore

# 로그 파일 및 디버그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 운영체제 및 편집기 관련
.DS_Store
Thumbs.db
.vscode
.idea

# 빌드 결과물
dist
build

# 테스트 커버리지, 캐시
coverage
.cache

# 환경 파일 (보안상 유출 방지)
.env
.env.local

# 프론트엔드 dockerignore
**/npm-debug.log*
**/yarn-error.log*
coverage/
.DS_Store
